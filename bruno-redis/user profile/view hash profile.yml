info:
  name: view hash profile
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:3000/api/user/:id/hash
  params:
    - name: id
      value: "123"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
