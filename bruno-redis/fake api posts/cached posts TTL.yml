info:
  name: cached posts TTL
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3000/api/posts/ttl
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
