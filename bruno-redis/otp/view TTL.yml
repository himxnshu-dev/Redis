info:
  name: view TTL
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:3000/api/otp/:phone/ttl
  params:
    - name: phone
      value: "+918580593241"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
