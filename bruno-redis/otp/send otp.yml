info:
  name: send otp
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/otp
  body:
    type: json
    data: |-
      {
        "phone": "+918580593241"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
