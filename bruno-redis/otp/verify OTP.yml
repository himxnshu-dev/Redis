info:
  name: verify OTP
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/otp/verify
  body:
    type: json
    data: |-
      {
        "phone": "+918580593241",
        "otp": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
