info:
  name: set redis banner key
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/banner
  body:
    type: json
    data: |-
      {
      //   "message": "welcome!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
