info:
  name: delete redis key-value
  type: http
  seq: 2

http:
  method: DELETE
  url: http://localhost:3000/api/banner
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
