info:
  name: user rank
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:3000/api/leaderboard/:userId/rank
  params:
    - name: userId
      value: "12"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
