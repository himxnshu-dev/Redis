info:
  name: update user score
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/leadership/score
  body:
    type: json
    data: |-
      {
        "userId": "1212",
        "score": 100
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
