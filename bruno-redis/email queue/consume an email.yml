info:
  name: consume an email
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3000/api/emails/process-one
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
